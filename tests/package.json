{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "client": "workspace:*",
    "db": "workspace:*",
    "server": "workspace:*",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.0",
    "typescript": "5.6.3",
    "vitest": "^3.1.2"
  }
}